# GitHub Setup

```powershell
git init
git add .
git commit -m "Release Windows IT Operations Toolkit v4.0"
git branch -M main
git remote add origin https://github.com/YOUR-USERNAME/windows-it-operations-toolkit.git
git push -u origin main
```

Recommended repository name:

```text
windows-it-operations-toolkit
```

Recommended topics:

`powershell`, `windows`, `it-support`, `helpdesk`, `sysadmin`, `active-directory`, `microsoft-365`, `microsoft-graph`, `automation`, `network-diagnostics`, `desktop-support`
